# Deploy GitHub Pages

1. Crie o repositório
2. Envie TODOS os arquivos desta pasta (incluindo `assets/`, `.nojekyll` e `.github/`)
3. Settings → Pages → Source: GitHub Actions
4. O workflow `deploy-pages.yml` publica sozinho no push em `main`

Sem backend. É só HTML/CSS/JS + imagens.
